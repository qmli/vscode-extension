1. Setting（设置）

定义：Setting 通常指的是程序或应用的特定运行配置项。它可以是用户调整的偏好、界面设置、功能启用/禁用等。一般来说，设置是与具体的用户或运行环境相关的参数。

特点：

更多地侧重于用户层面的配置。

设置可能是临时的，用户在使用时可能会频繁更改它们。

比如应用程序的语言选择、主题（黑暗模式或浅色模式）、字体大小等。

示例：

在一个编辑器中，用户可以设置字体大小、背景颜色、快捷键等。

在 Web 应用中，用户可能有设置选项来控制通知频率、隐私权限等。
