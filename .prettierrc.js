module.exports = {
  // 每行最大长度，超出会自动换行
  printWidth: 120,

  // 每个缩进级别的空格数
  tabWidth: 2,

  // 是否用 tab 字符缩进。false 表示用空格
  useTabs: false,

  // 语句末尾是否加分号
  semi: true,

  // 是否使用单引号（true 单引号，false 双引号）
  singleQuote: true,

  // 尾随逗号的用法（"none"：不加；"all"：都加；"es5"：ES5 支持的地方加）
  trailingComma: 'none',

  // 对象字面量中的括号是否有空格（如 { foo: bar }）
  bracketSpacing: true,

  // 箭头函数参数是否总是带括号（如 (x) => ...）
  arrowParens: 'always',

  // 统一行尾风格：'lf' 表示换行用 LF（Unix），不是 CRLF（Windows）
  endOfLine: 'lf'
};
